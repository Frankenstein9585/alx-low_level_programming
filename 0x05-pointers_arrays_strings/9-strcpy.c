#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strcpy - print array up to n elements
 * @dest: array to be read
 * @src: number to be read
 * Return: always 0
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
