#include "main.h"
#include <string.h>
/**
  * _strlen - returns the length of a string
  * @s: string to be operated on
  * Return: the length of string
  */
int _strlen(char *s)
{
	return (strlen(*s));
}
